/// Contains an example command handler that performs a `Task` by invoking a `TaskProvider`. The
/// `Task` is a contrived example that lists the Package URL for each [Package] in the data store
/// to stdout. This provides an example of how to structure and develop a `TaskProvider`.
///
/// This template can be used as a starting point for building a custom extension or as a model
/// for how to author a general use `TaskProvider` for Harbor.
pub mod example;
